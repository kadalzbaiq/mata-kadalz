# Repo Context — ubiquitous language

This project's glossary and nothing else. Implementation details, specs, and decisions do NOT belong here.

## Glossary

_Fill in when a domain term is settled (via domain-modeling): what a thing is in 1-2 sentences, rejected synonyms under `_Avoid_`._

<!-- Example:
## Order

A purchase transaction that starts when the cashier presses "Create Order" and ends when payment is settled.

_Avoid_: Purchase, Transaction, Checkout
-->

## Architecture Decisions

See `docs/adr/` for hard-to-reverse decisions. Format: `NNNN-slug.md` (context, choice, reason — 1-3 sentences).